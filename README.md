# dsplib
DSP library for MSP430 provided by TI, adapted to operate with [CMU maker](https://github.com/CMUAbstract/maker). 

Define the `MSP_LEA_USE_ISR` flag to make sure the isr gets initilized by this library.
